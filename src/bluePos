setAutonSpeeds();
mogomech.set(false);
Drivetrain.setDriveVelocity(30.5, percent);
  backwardDrive(4.6);
  wait(1.5, sec);
  Drivetrain.setDriveVelocity(30.5, percent);
  mogomech.set(true);
  wait(0.4, sec);
  contakeForward();
  wait(1, sec);
  conveyor.stop();
  wait(0.2,sec);
  turnRight(1);
  wait(1.25, sec);
  forwardDrive(2.3);
  wait(1,sec);
  conveyor.spin(fwd);
  wait(0.5,sec);

  // turnLeft(1.85); // gets alliance ring
  // wait(0.7,sec);
  // intakePiston.set(true);
  // conveyor.stop();
  // forwardDrive(5.2);
  // wait(2.5,sec);
  // intakePiston.set(false);
  // conveyor.spin(fwd);
  // wait(0.3,sec);
  // Drivetrain.setDriveVelocity(50, percent);

  // backwardDrive(3); //touches tower AFTER ALLIANCE RING
  // wait(1,sec);
  // turnLeft(1);
  // wait(1,sec);
  // forwardDrive(2); // 

  // backwardDrive(3); //sets up for third stake AFTER ALLIANCE RING
  // wait(1,sec);
  // turnLeft(0.8);
  // wait(0.8,sec);
  // mogomech.set(false);
  // backwardDrive(3.2);
  // wait(1,sec);
  // turnRight(1.6);
  // wait(1,sec);
  // backwardDrive(1.4);
  
  Drivetrain.setDriveVelocity(50, percent); // sets up for third stake NO ALLIANCE RING (clears corner)
  turnLeft(0.95);
  wait(0.85,sec);
  conveyor.stop();
  forwardDrive(3);
  wait(1.4,sec);
  mogomech.set(false);
  conveyor.spin(fwd);
  forwardDrive(2);
  wait(0.48,sec);
  turnRight(1.5);
  wait(0.9,sec);
  arm_up_down();
  wait(0.4,sec);
  forwardDrive(2);
  wait(1,sec);
  Drivetrain.setDriveVelocity(50, percent);
  turnLeft(2.2);
  wait(1.4,sec);
  arm_up_down();
  wait(1,sec);
  backwardDrive(4.2);
